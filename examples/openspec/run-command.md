# Run Command

```bash
conductor run examples/openspec/openspec-pipeline.yaml \
  --input change="add-openspec-pipeline-readme" \
  --input idea="Add a README.md to examples/openspec/ that explains the pipeline structure, how to run it, and what each phase does"
```
