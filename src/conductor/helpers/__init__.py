"""Helpers package for engine-agnostic Conductor utilities.

Currently exposes the :mod:`conductor.helpers.error` subpackage. The
``helpers`` namespace is reserved for future additions (logging,
retry, metrics) that follow the same engine-agnostic pattern.
"""

from __future__ import annotations
